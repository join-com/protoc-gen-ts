version: 2.1

docker-auth: &docker-auth
  auth:
    username: ${DOCKER_LOGIN}
    password: ${DOCKER_PASSWORD}

commands:
  install_protoc:
    description: Install protobuf's protoc generator
    steps:
      - run:
          command: sudo apt install -y protobuf-compiler
  compile_and_test:
    description: Compile & test protoc-gen-ts
    steps:
      - checkout
      - run:
          name: Install test dependencies
          command: cd ./integrationTests && yarn
      - run:
          name: Compile
          command: cd ./integrationTests && yarn compile
      - run:
          name: Generate TS code
          command: cd ./integrationTests && yarn proto:build
      - run:
          name: Test
          command: cd ./integrationTests && yarn test

jobs:
  build_and_test:
    docker:
      - image: circleci/golang:1.16-node
        <<: *docker-auth
    steps:
      - install_protoc
      - do_all
